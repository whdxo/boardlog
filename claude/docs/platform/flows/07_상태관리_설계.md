# 상태 관리 설계 — Zustand Stores

---

## 설계 원칙

- **서버 상태** (API 데이터): TanStack Query로 관리 (캐싱, 리페치, 낙관적 업데이트)
- **클라이언트 상태** (UI, 인증, 사용자 설정): Zustand로 관리
- **URL 상태** (필터, 탭, 페이지): searchParams로 관리 (공유 가능)

---

## 1. Auth Store

```typescript
// stores/authStore.ts
interface AuthState {
  user: User | null
  isLoggedIn: boolean
  isLoading: boolean

  // 액션
  setUser: (user: User | null) => void
  logout: () => void
}

// 사용 예
const { user, isLoggedIn } = useAuthStore()

// 로그인 필요 시
const requireAuth = (action: () => void) => {
  if (!isLoggedIn) {
    useUIStore.getState().openLoginSheet()
    return
  }
  action()
}
```

---

## 2. UI Store

```typescript
// stores/uiStore.ts
interface UIState {
  // 로그인 유도 시트
  isLoginSheetOpen: boolean
  loginSheetCallbackUrl: string | null

  // 컬렉션 상태 변경 시트
  isCollectionSheetOpen: boolean
  collectionSheetGameId: string | null

  // 평점 모달
  isRatingModalOpen: boolean
  ratingModalGameId: string | null

  // 검색 모달
  isSearchModalOpen: boolean

  // 액션
  openLoginSheet: (callbackUrl?: string) => void
  closeLoginSheet: () => void
  openCollectionSheet: (gameId: string) => void
  closeCollectionSheet: () => void
  openRatingModal: (gameId: string) => void
  closeRatingModal: () => void
  openSearchModal: () => void
  closeSearchModal: () => void
}
```

---

## 3. Collection Store

```typescript
// stores/collectionStore.ts
// 낙관적 업데이트용 로컬 상태

interface CollectionState {
  // 게임ID → 컬렉션 상태 맵 (서버에서 초기화)
  statuses: Record<string, CollectionStatus | null>
  // 게임ID → 내 별점
  ratings: Record<string, number | null>

  // 액션
  setStatus: (gameId: string, status: CollectionStatus | null) => void
  setRating: (gameId: string, rating: number | null) => void
  initFromServer: (collections: Collection[], ratings: Rating[]) => void
}

// 사용 예
const status = useCollectionStore(s => s.statuses[gameId])
const setStatus = useCollectionStore(s => s.setStatus)
```

---

## 4. Filter Store

```typescript
// stores/filterStore.ts
// 게임 탐색 필터 (URL searchParams와 동기화)

interface FilterState {
  games: GameFilter
  shop: ShopFilter
  used: UsedFilter

  setGameFilter: (filter: Partial<GameFilter>) => void
  resetGameFilter: () => void
  setShopFilter: (filter: Partial<ShopFilter>) => void
  setUsedFilter: (filter: Partial<UsedFilter>) => void
}

interface GameFilter {
  search?: string
  players?: string
  priceRange?: string
  genres?: string[]
  playTime?: string
  minAge?: string
  sortBy?: string
}

interface ShopFilter {
  category?: 'game' | 'accessory'
  minPrice?: number
  maxPrice?: number
  wishlistOnly?: boolean
}

interface UsedFilter {
  type?: UsedType
  gameId?: string
}
```

---

## 5. TanStack Query 키 설계

```typescript
// lib/queryKeys.ts
export const queryKeys = {
  // 게임
  games: {
    all: ['games'] as const,
    list: (filter: GameFilter) => ['games', 'list', filter] as const,
    detail: (id: string) => ['games', 'detail', id] as const,
    ranking: (period: string) => ['games', 'ranking', period] as const,
    ratings: (id: string) => ['games', 'ratings', id] as const,
    plays: (id: string) => ['games', 'plays', id] as const,
  },

  // 커뮤니티
  posts: {
    all: ['posts'] as const,
    list: (filter: PostFilter) => ['posts', 'list', filter] as const,
    detail: (id: string) => ['posts', 'detail', id] as const,
    comments: (id: string) => ['posts', 'comments', id] as const,
  },

  // 내 게임
  my: {
    collection: (userId: string) => ['my', 'collection', userId] as const,
    plays: (userId: string) => ['my', 'plays', userId] as const,
    ratings: (userId: string) => ['my', 'ratings', userId] as const,
    selections: (userId: string) => ['my', 'selections', userId] as const,
    stats: (userId: string) => ['my', 'stats', userId] as const,
  },

  // 로컬
  local: {
    list: (filter: LocalFilter) => ['local', 'list', filter] as const,
    detail: (id: string) => ['local', 'detail', id] as const,
    reviews: (id: string) => ['local', 'reviews', id] as const,
  },

  // 쇼핑
  shop: {
    list: (filter: ShopFilter) => ['shop', 'list', filter] as const,
    detail: (id: string) => ['shop', 'detail', id] as const,
  },

  // 중고
  used: {
    list: (filter: UsedFilter) => ['used', 'list', filter] as const,
    detail: (id: string) => ['used', 'detail', id] as const,
    priceHistory: (gameId: string) => ['used', 'price', gameId] as const,
  },

  // 알림
  notifications: ['notifications'] as const,

  // 유저
  user: {
    profile: (id: string) => ['user', 'profile', id] as const,
    followers: (id: string) => ['user', 'followers', id] as const,
  },
} as const
```

---

## 6. API 레이어 설계

```typescript
// lib/api/games.ts
export const gamesApi = {
  getList: (filter: GameFilter) =>
    fetch(`/api/games?${qs.stringify(filter)}`).then(r => r.json()),

  getDetail: (id: string) =>
    fetch(`/api/games/${id}`).then(r => r.json()),

  getRanking: (period: 'weekly' | 'monthly' | 'all') =>
    fetch(`/api/games/ranking?period=${period}`).then(r => r.json()),
}

// lib/api/collection.ts
export const collectionApi = {
  getMyCollection: () =>
    fetch('/api/my/collection').then(r => r.json()),

  setStatus: (gameId: string, status: CollectionStatus | null) =>
    fetch(`/api/my/collection/${gameId}`, {
      method: 'PUT',
      body: JSON.stringify({ status }),
    }).then(r => r.json()),
}

// lib/api/index.ts
export * from './games'
export * from './collection'
export * from './posts'
export * from './local'
export * from './shop'
export * from './used'
export * from './auth'
```

---

## 7. 인증 미들웨어 (Next.js)

```typescript
// middleware.ts
// 로그인 필요 경로 정의
const AUTH_REQUIRED_PATHS = [
  '/my',
  '/community/new',
  '/used/write',
  '/meetup/create',
  '/settings',
  '/profile/edit',
]

export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl

  const isAuthRequired = AUTH_REQUIRED_PATHS.some(path =>
    pathname.startsWith(path)
  )

  if (isAuthRequired && !isAuthenticated(request)) {
    return NextResponse.redirect(
      new URL(`/login?callbackUrl=${pathname}`, request.url)
    )
  }
}
```

---

## 8. 낙관적 업데이트 패턴

```typescript
// 컬렉션 추가 시 낙관적 업데이트 예시
const { mutate } = useMutation({
  mutationFn: (status: CollectionStatus) =>
    collectionApi.setStatus(gameId, status),

  onMutate: async (status) => {
    // 1. 진행 중인 리페치 취소
    await queryClient.cancelQueries({ queryKey: queryKeys.my.collection(userId) })

    // 2. 이전 상태 스냅샷
    const previous = queryClient.getQueryData(queryKeys.my.collection(userId))

    // 3. 낙관적으로 즉시 업데이트
    useCollectionStore.getState().setStatus(gameId, status)

    return { previous }
  },

  onError: (err, status, context) => {
    // 실패 시 롤백
    useCollectionStore.getState().setStatus(gameId, context?.previous ?? null)
    toast.error('저장에 실패했어요')
  },

  onSuccess: () => {
    toast.success('컬렉션에 추가했어요')
    queryClient.invalidateQueries({ queryKey: queryKeys.my.collection(userId) })
  },
})
```
